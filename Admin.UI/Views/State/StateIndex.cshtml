@model IEnumerable<StateDto>
@using Newtonsoft.Json


<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="text-white">State List</h1>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-action="StateCreate" class="btn btn-outline-primary"><i class="bi bi-plus-square"></i> Create New State</a>
            </div>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        State Id
                    </th>
                    <th>
                        State Name
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>

                        <td>@item.StateId</td>
                        <td>@item.StateName</td>
                        <td>

                            <a asp-action="StateToUpdate" asp-route-stateid="@item.StateId" class="btn btn-primary">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <a asp-action="StateDelete" asp-route-stateid="@item.StateId" class="btn btn-danger">
                                <i class="bi bi-trash"></i>
                            </a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@section Scripts
{

    <partial name="_ValidationScriptsPartial" />
}