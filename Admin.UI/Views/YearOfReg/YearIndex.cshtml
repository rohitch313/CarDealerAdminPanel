@model IEnumerable<PvaYearOfRegDto>
@using Newtonsoft.Json


<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="text-white">Year List</h1>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-action="YearCreate" class="btn btn-outline-primary"><i class="bi bi-plus-square"></i> Create New Year</a>
            </div>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Year Id
                    </th>
                    <th>
                        Year
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>

                        <td>@item.YearId</td>
                        <td>@item.YearCode</td>
                       
                        <td>

                            <a asp-action="YearToUpdate" asp-route-stateid="@item.YearId" class="btn btn-primary">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <a asp-action="YearDelete" asp-route-stateid="@item.YearId" class="btn btn-danger">
                                <i class="bi bi-trash"></i>
                            </a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@section Scripts
{

    <partial name="_ValidationScriptsPartial" />
}